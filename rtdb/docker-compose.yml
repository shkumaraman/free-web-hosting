services:
  rtdb:
    build: .
    container_name: rtdb
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./db_data:/data
    environment:
      - STORAGE_DIR=/data
      - HF_TOKEN=${HF_TOKEN}
      - HF_REPO_ID=${HF_REPO_ID}
      - TF_CPP_MIN_LOG_LEVEL=2
